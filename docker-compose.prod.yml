# Production Override
# Use: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build

services:
  backend:
    network_mode: host
    environment:
      - DATABASE_HOST=localhost
      - PORT=5001
